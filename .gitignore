*.pyc
.env
.env.*
# ...except the template install copies. It holds no secrets, only the
# variable names, and a fresh clone is unusable without it.
!.env.example
.venv/
__pycache__/
audio_cache/
backups/
cache/
hermes-agent-self-evolution/
hermes-agent/
logs/
skills/.hub/
state.db
state.db-*

# --- runtime state. None of this is the build. ---
# auth.json holds live API credentials for four providers. It was committed by
# accident in 10d3973 and every commit after. Never track it.
auth.json
auth.lock
.update_check
.skills_prompt_snapshot.json
models_dev_cache.json
models_dev_cache.etag
state.db
cache/
sessions/
sandboxes/
audio_cache/
image_cache/
pairing/
backups/
memories/
.worktrees/
cron/.jobs.lock
skills/.usage.json
skills/.usage.json.lock
# graphify writes here from a commit hook. Another tool's output, not ours.
graphify-out/
# the pinned upstream install and its venv: 1.1GB, reinstallable from PINNED_VERSION
hermes-agent/
hermes-agent-self-evolution/
.venv/

# --- generated from templates/ + estate.yaml by bin/render ---
# Do not edit these directly. Edit the matching file in templates/.
estate.yaml
CUTOVER.md
MEMORY.md
REQUIREMENTS.jsonl
RITUALS.md
bin/prove-watch-readonly.sh
bin/pulse.sh
cron/watch.jobs
cron/work.jobs
egress-allowlist.txt
handlers/screenshot_to_issue.py
profiles/watch/MEMORY.md
profiles/watch/egress-allowlist.txt
profiles/work/MEMORY.md
scripts/pulse.sh
skills/PLATFORM_GATING.md
skills/estate-map/SKILL.md
skills/incident-triage/SKILL.md
skills/post-mortem/SKILL.md
skills/pr-discipline/SKILL.md
skills/screenshot-to-story/SKILL.md
skills/verify-to-prod/SKILL.md
cron/jobs.json
# Scheduler state, written on every run. It is a live database, not source.
cron/executions.db
cron/output/
cron/.tick.lock
cron/.fire-*.lock
# Skills the agent ships with, unpacked into skills/ at first run. Ours are
# generated from templates/ and listed above; skills/VETTED.md is the only
# file here anyone writes by hand.
skills/.bundled_manifest
skills/*/
!skills/estate-map/
!skills/incident-triage/
!skills/post-mortem/
!skills/pr-discipline/
!skills/screenshot-to-story/
!skills/verify-to-prod/
estate-evals/incidents.jsonl
