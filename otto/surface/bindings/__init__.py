"""Concrete surface bindings (spec bullet 5): two examples proving the
``SurfaceAdapter`` contract is agnostic, not Telegram-shaped in disguise.
Both are pure functions — no network call, no token, no server.
"""

from __future__ import annotations
