# Every third-party package the otto/ packages import, pinned to the exact
# version proved green by the otto test suite. Every entry is an exact
# ``==`` pin: an unpinned name here is a build that changes under us.
# The guard is otto/tests/integration/test_requirements_pinned.py.

# runtime
asyncpg==0.31.0
cryptography==50.0.0
jsonschema==4.25.1
nats-py==2.15.0
opentelemetry-api==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0
opentelemetry-sdk==1.37.0
psycopg==3.3.4
pydantic==2.13.4
python-ulid==4.0.1
PyYAML==6.0.3

# test-only
pytest==9.0.3
pytest-bdd==8.1.0
